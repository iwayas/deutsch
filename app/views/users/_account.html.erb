<div class="show-user">
  <img src="<%= "/user_images/#{@user.image_name}" %>" value="<%= @user.image_name %>"> 
</div>
<div class="show-account">
  <h3><%= @user.name %></h3>
  <div class="show-account_edit">
    <% if @user.id == @current_user.id %>
      <%= link_to("アカウント編集", "/users/#{@user.id}/edit") %>
    <% end %>
  </div>
</div>